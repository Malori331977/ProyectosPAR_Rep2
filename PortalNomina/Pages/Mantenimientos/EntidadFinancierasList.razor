@page "/entidadfinancieraslist"

@layout MainLayout

<div class="card">
    <PageMant Titulo="@Titulo" OnClickAddSelected="OnClickAdd" />
    <div class="card-body">
        @if (InProcess)
        {
            <SpinnerControl></SpinnerControl>
        }
        <Grid @ref="grid" TItem="EntidadFinanciera" class="table table-hover table-bordered table-striped "
              DataProvider="modelDataProvider" AllowFiltering="true" Responsive="true"
              AllowPaging="false" PageSize="10" PageSizeSelectorVisible="false" PageSizeSelectorItems="@(new int[] {10,50,100,200,500})"
              PaginationItemsTextFormat="{0} - {1} of {2} items" ItemsPerPageText=""
              HeaderRowCssClass="custom-header border-bottom-0"
              FiltersRowCssClass="custom-filter text-white border-bottom-0"
              FiltersTranslationProvider="gridExtensions.GridFiltersTranslationProvider"
              AllowRowClick="true"
              FixedHeader="true"
              Height="500"
              Unit="Unit.Px"
              OnRowDoubleClick="OnSelect">

            <GridColumn TItem="EntidadFinanciera" HeaderText="Id" PropertyName="Id" Style="width:200px">
                @context.Id
            </GridColumn>
            <GridColumn TItem="EntidadFinanciera" HeaderText="Descripción" PropertyName="Descripcion">
                @context.Descripcion
            </GridColumn>
            <GridColumn TItem="EntidadFinanciera" HeaderText="Activo" PropertyName="Activo">
                <Switch Class="ml-5" @bind-Value="@context.Activo" Disabled />
            </GridColumn>
            <GridColumn TItem="EntidadFinanciera" HeaderText="Acción" PropertyName="action" Filterable="false" TextAlignment="Alignment.Center">
                <button class="btn btn-danger rounded-circle text-white" type="submit" @onclick="@((e)=> OnDelete(e, context))">
                    <Icon Name="IconName.Trash" Size="IconSize.x6" />
                </button>
            </GridColumn>
        </Grid>
    </div>
</div>

@code {
    public string Titulo { get; set; }

    BlazorBootstrap.Grid<EntidadFinanciera> grid;
    private IEnumerable<EntidadFinanciera> dataList;
    private bool InProcess = true;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (Titulo is null) Titulo = "Entidades Financieras";
        await base.OnAfterRenderAsync(firstRender);
    }

    private async Task<GridDataProviderResult<EntidadFinanciera>> modelDataProvider(GridDataProviderRequest<EntidadFinanciera> request)
    {
        if (dataList is null)
            dataList = await GetDataList();
        return await Task.FromResult(request.ApplyTo(dataList));
    }

    private async Task<IEnumerable<EntidadFinanciera>> GetDataList()
    {
        List<EntidadFinanciera> model = new();
        try
        {
            model = (await mtoService.GetEntidadFinanciera()).ToList();
        }
        catch (Exception)
        {
            await swal.FireAsync("Error", "Error al cargar la información solicitada.", SweetAlertIcon.Info);
        }

        InProcess = false;
        StateHasChanged();
        return model;
    }

    private async Task OnClickAdd()
    {
        NavigationManager.NavigateTo("./entidadfinancierasdetalle/0/1");
    }
    protected async void OnSelect(GridRowEventArgs<EntidadFinanciera> args)
    {
        NavigationManager.NavigateTo("./entidadfinancierasdetalle/" + args.Item.Id + "/2");
    }
    protected async void OnDelete(MouseEventArgs mouseEventArgs, EntidadFinanciera args)
    {
        NavigationManager.NavigateTo($"./entidadfinancierasdetalle/{args.Id}/3");
    }
}


